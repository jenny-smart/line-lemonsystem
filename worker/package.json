{
  "name": "line-lemon-webhook",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@libsql/client": "^0.14.0"
  },
  "devDependencies": {
    "wrangler": "^4.113.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test"
  }
}
